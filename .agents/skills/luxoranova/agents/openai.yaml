interface:
  display_name: "Luxoranova"
  short_description: "Repo-specific patterns and workflows for luxoranova"
  default_prompt: "Use the luxoranova repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true